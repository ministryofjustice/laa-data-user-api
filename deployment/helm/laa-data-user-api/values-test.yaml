ingress:
  # TODO: set to the current value of the `INGRESS_CLASS_NAME` GitHub
  # Environment variable for `test` (Settings > Environments >
  # test > Variables), once the test namespace is provisioned.
  className: "CHANGEME-test-ingress-class"
