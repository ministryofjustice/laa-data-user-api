ingress:
  # TODO: set to the current value of the `INGRESS_CLASS_NAME` GitHub
  # Environment variable for `development` (Settings > Environments >
  # development > Variables) before merging.
  className: "modsec-non-prod"
